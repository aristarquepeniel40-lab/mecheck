% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_rules.R
\name{me_check_rule}
\alias{me_check_rule}
\title{Regle de controle qualite}
\usage{
me_check_rule(
  name = character(0),
  check_fn = function() NULL,
  severity = character(0)
)
}
\arguments{
\item{name}{Nom court de la regle (ex. "dates_coherentes").}

\item{check_fn}{Fonction \verb{function(project) -> NULL | character(1)}.}

\item{severity}{\code{"error"} ou \code{"warning"} — indicatif seulement, n'a
pas d'effet sur l'execution (voir \code{run_checks()}).}
}
\description{
Une regle est une fonction qui prend un \code{mecore::me_project} et
retourne \code{NULL} (regle respectee) ou un message \code{character} decrivant
le probleme. Contrairement a \code{mecore::me_validate()}, une regle
\code{mecheck} ne LEVE jamais d'erreur — elle produit un constat, pour
permettre un audit complet meme si plusieurs regles echouent a la fois.
}
